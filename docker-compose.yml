services:
  bsimgbed:
    image: ilolioo/bsimgbed:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bsimgbed
    restart: unless-stopped
    ports:
      - "5198:5198"
    volumes:
      - ./db:/app/db
      - ./uploads:/app/uploads
    environment:
      NODE_ENV: production
      HOST: "0.0.0.0"
      PORT: "5198"
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
